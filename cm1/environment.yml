name: cm1
channels:
  - conda-forge
  - defaults
dependencies:
  - python
  - pip
  - conda-forge::nwsspc-sharplib  # not on PyPI, must stay here
  - pip:
    - -e ..  # installs pyproject.toml dependencies
prefix: /glade/work/ahijevyc/conda-envs/cm1
